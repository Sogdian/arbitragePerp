# CONTEXT: Фандинг-арбитраж между биржами (scan_fundings_spreads.py)

## Обзор

Модуль **`scan_fundings_spreads.py`** — бот для поиска пар бирж и монет по стратегии фандинг-арбитража между двумя биржами. Он только **мониторит** возможности и шлёт уведомления в консоль и Telegram; открытие и закрытие ордеров планируется реализовать отдельно.

---

## 1. Стратегия фандинг-арбитража

### Общая концепция

- **Биржа 1 (Long):** фандинг сильно отрицательный (например −2%). В лонге мы **получаем** фандинг от шортистов.
- **Биржа 2 (Short):** фандинг от 0 до ~0.5% — хедж; в шорте мы **платим** не больше ~0.5%.
- За некоторое время до выплаты фандинга на бирже 1 входим в **Long** на бирже 1 и в **Short** на бирже 2.
- В момент выплаты на бирже 1 получаем фандинг (например −2%), на бирже 2 платим от 0 до 0.5%.
- После выплаты закрываем обе позиции.

### Детали стратегии

1. **Мониторинг фандингов** — постоянно проверяются актуальные ставки на двух биржах. Если фандинги меняются и сделка перестаёт быть прибыльной, бот предупреждает, чтобы не входить.
2. **Спред цен** — мониторинг следит, чтобы спред цен между биржами был минимальным. Вход и выход — в моменты минимального спреда; сделки лимитными ордерами.
3. **Формула спреда фандинга (наша выгода):**  
   получаем на Long (при отрицательном rate — модуль) − платим на Short (модуль rate) в процентах.  
   Примеры: Long −2%, Short +0.5% → Фанд = 2% − 0.5% = **1.5%**; Long −2%, Short −0.23% → Фанд = 2% − 0.23% = **1.77%**.

---

## 2. Бот scan_fundings_spreads.py

### Назначение

- Находить пары **(биржа Long, биржа Short)** и монеты: первичный отбор — по фандингу на Long (**≤ MIN_FUNDING_LONG_FILTER_FOR_LOG**); при анализе проверяются спред фандинга **≥ MIN_FUNDING_SPREAD**, спред цен по модулю **≤ MAX_PRICE_SPREAD** и время до выплаты на Long.
- Проводить анализ ликвидности и новостей (делистинг, безопасность) и выводить вердикт: ✅ арбитражить / ❌ не арбитражить.
- Писать одну строку в **консоль** на каждую подходящую пару (без записи в файл).
- Отправлять в **Telegram** одно сообщение на монету (картинка-таблица + подпись), только если **время до выплаты на Long бирже** **< SCAN_FUNDING_MIN_TIME_TO_PAY** минут (главная биржа — лонг, нужна выплата на лонг в ближайшее время).

### Условия отбора пар

**В process_coin (первичный фильтр):** для каждой монеты перебираются пары бирж `(ex1, ex2)` из бирж с валидными bid/ask. В лог на анализ попадают только связки, где **фандинг на бирже Long (в %) ≤ MIN_FUNDING_LONG_FILTER_FOR_LOG** (например −0.5: проходят −1%, −1.5%; не проходят −0.4%, 0%, +1%):

- **Long на ex1, Short на ex2:** добавляется, если `funding_rate(ex1) * 100 <= MIN_FUNDING_LONG_FILTER_FOR_LOG`.
- **Long на ex2, Short на ex1:** добавляется, если `funding_rate(ex2) * 100 <= MIN_FUNDING_LONG_FILTER_FOR_LOG`.

На этом этапе **не** проверяются MIN_FUNDING_SPREAD и MAX_PRICE_SPREAD — они применяются в **\_analyze_and_log_opportunity**.

**В _analyze_and_log_opportunity** для каждой такой пары **сначала** выполняется ранняя отсечка (без вызова ликвидности и новостей). При невыполнении — лог с причиной и выход (без ликвидности и новостей).

**Ранняя отсечка (порядок проверок):**

1. **Спред цен (данные)** — есть данные (open_spread_pct не None). Иначе в лог: «спред цен: нет данных».
2. **Цена Long ниже, чем Short** — ask_long < bid_short (цена на Long бирже ниже, чем на Short). Иначе в лог: «цена на Long выше чем на Short».
3. **Спред фандинга (данные)** — есть данные (funding_spread_val не None). Иначе в лог: «фандинг: нет данных».
4. **Спред фандинга (порог)** — funding_spread_val ≥ MIN_FUNDING_SPREAD. Иначе в лог: «фандинг X% < Y%».
5. **Время до выплаты на Long** — m_long < SCAN_FUNDING_MIN_TIME_TO_PAY. Иначе в лог: «время выпл. на Long X мин >= Y мин».

Только если все пять проверок пройдены, вызываются **check_liquidity** и **_get_news_cached**; затем выносится вердикт ✅/❌ и пишется полная строка в лог. Ранняя отсечка экономит ресурсы на заведомо неподходящих парах.

### Формулы

| Величина | Формула |
|----------|---------|
| Спред фандинга (%), наша выгода | получаем на Long − платим на Short: `(receive_long - pay_short) * 100`, где receive_long = −funding_long при funding_long < 0, pay_short = \|funding_short\| |
| Спред по цене (%) | `(bid_short - ask_long) / ask_long * 100` |
| Общий спред (%) | `spread_price + spread_funding` |

Пример: Long −2%, Short +0.5% → спред фандинга 1.5%. Спред по цене 0.8% → общий спред 2.3%.

### Поток данных

1. **Сбор монет:** `collect_coins_by_exchange` — список фьючерсных монет по каждой бирже (union, без EXCLUDE_COINS и цифровых префиксов).
2. **Fetch по монете:** для каждой биржи, где есть монета, запрашиваются:
   - тикер (`get_futures_ticker`) — bid, ask, price;
   - фандинг (`get_funding_info` при наличии, иначе `get_funding_rate`) — `funding_rate`, `next_funding_time` (для расчёта минут до выплаты).
3. **Фильтр пар (process_coin):** для всех пар бирж с валидными данными остаются только связки, где **фандинг на бирже Long (в %) ≤ MIN_FUNDING_LONG_FILTER_FOR_LOG**. Спред фандинга и спред цен на этом шаге не проверяются.
4. **Анализ:** для каждой такой пары вызывается `_analyze_and_log_opportunity`:
   - **Ранняя отсечка:** проверка данных по спреду цен; правило «цена на Long ниже чем на Short» (ask_long < bid_short); данные и порог по фандингу; время до выплаты на Long. При невыполнении — лог с причиной и выход **без** вызова ликвидности и новостей.
   - Если все проверки ранней отсечки пройдены: ликвидность (`check_liquidity` для Long и Short), новости (кеш по `(coin, exchange)`; делистинг и безопасность по обеим биржам), затем вердикт и лог.
5. **Лог:** одна строка в консоль на пару в формате:
   - `{COIN} Long (ex1), Short (ex2) Спред цен: X.XXX% | Фанд: Y.YYY% (L: A мин | S: B мин) | Общий: Z.ZZZ% ✅ арбит. / ❌ не арбит. (причина или кол-во монет)`.
   - L/S — время до выплаты на бирже Long и Short; при ранней отсечке в скобках указывается причина (спред цены, фандинг, время выпл.).
   - В конце цикла: «Цикл: пар на анализ 0 (...)» или «Цикл: пар на анализ (с вердиктами): N». В начале каждого цикла в Telegram отправляется «🔄 Новый цикл поиска спреда фандингов».
6. **Telegram:** среди возможностей с вердиктом ✅ отбираются те, у которых **время до выплаты на Long бирже** **< SCAN_FUNDING_MIN_TIME_TO_PAY**. По каждой монете — одно сообщение: картинка-таблица + подпись (на русском); при отсутствии PIL — текст `_format_combined_telegram_message`. Ссылки на биржи: OKX — `ru/trade-swap/{coin}-usdt-swap`.

### Переменные окружения (.env)

| Переменная | Смысл | Пример |
|------------|--------|--------|
| **MIN_FUNDING_SPREAD** | Минимальный спред фандинга (%), проверяется в ранней отсечке; ниже — «не арбит» | 1.5, 2 |
| **MIN_FUNDING_LONG_FILTER_FOR_LOG** | Первичный фильтр в process_coin: в лог на анализ попадают только связки, где фандинг на Long (в %) ≤ этого значения (отриц. = получаем на лонг) | −0.5 |
| **MAX_PRICE_SPREAD** | Максимальный допустимый спред цен по модулю (%), проверяется в ранней отсечке | 0.5, 1.5, 2 |
| **SCAN_FUNDING_MIN_TIME_TO_PAY** | Порог минут до выплаты на Long: ранняя отсечка если L ≥ этого значения; в Telegram шлём только если L < этого значения | 60 |
| **SCAN_FUNDING_INTERVAL_SEC** | Интервал сканирования (сек) | 60 |
| **SCAN_FUNDING_MAX_CONCURRENCY** | Параллелизм HTTP-запросов | 20 |
| **SCAN_FUNDING_COIN_BATCH_SIZE** | Размер батча монет за проход | 50 |
| **SCAN_FUNDING_REQ_TIMEOUT_SEC** | Таймаут запросов (сек) | 20 |
| **SCAN_FUNDING_MEXC_REQ_TIMEOUT_SEC** | Таймаут для MEXC (сек) | 45 |
| **SCAN_COIN_INVEST** | Объём в USDT для проверки ликвидности и расчёта кол-ва монет | 50 |
| **SCAN_NEWS_CACHE_TTL_SEC** | TTL кеша новостей (сек) | 180 |
| **SCAN_ANALYSIS_MAX_CONCURRENCY** | Параллелизм анализа (ликвидность + новости) | 2 |
| **EXCLUDE_COINS** | Монеты через запятую для исключения | FLOW,BTC |
| **EXCLUDE_EXCHANGES** | В коде зашито `{"lbank"}` | — |

Используются также **config** (TEST_CHANNEL_ID, BOT_TOKEN, ENABLE_TELEGRAM и т.д.) и общий **PerpArbitrageBot** (биржи, news_monitor, announcements_monitor, x_news_monitor).

### Логирование

- Только консоль (StreamHandler), **без записи в файл**.
- Формат строки: `%(asctime)s - %(message)s` (уровень и имя логгера в вывод не выводятся).
- Имя логгера: `scan_fundings_spreads`.
- Подробные логи от httpx, bot, news_monitor, exchanges и т.д. заглушены (WARNING/CRITICAL).

### Зависимости

- **bot.PerpArbitrageBot** — биржи, новости.
- **telegram_sender.TelegramSender** — отправка в канал (TEST_CHANNEL_ID).
- **config** — ENV_MODE, TEST_CHANNEL_ID и др.
- Биржи с методом **get_funding_info** (funding_rate + next_funding_time); при его отсутствии используется **get_funding_rate** (тогда `minutes_until` будет None и в Telegram по этой паре не попадёт).

### Запуск

```bash
python3 scan_fundings_spreads.py
```

Цикл: обновление списка монет → `scan_once` по батчам → пауза **SCAN_FUNDING_INTERVAL_SEC** секунд.

---

## 3. Бот scan_fundings_spreads_bot.py (открытие и закрытие одной пары)

### Назначение

Интерактивный бот для **одной** пары Long/Short: ввод строки → анализ → вопрос → при ответе «Да, X» открытие позиций и мониторинг до срабатывания порога закрытия → отправка в Telegram и закрытие этой пары. Вся логика открытия/закрытия и Telegram вызывается из **bot.py** и **position_opener** (импорт функций).

### Ввод

- Один запрос в формате, как в **bot.py**: `"STO Long (mexc), Short (bybit) 30"` (монета, Long биржа, Short биржа, количество монет).
- Парсинг через **input_parser.parse_input** (тот же формат, что и в bot.py).

### Поток

1. **Анализ:** вывод заголовка «Анализ арбитража для {монета}» и полный анализ как в **bot.py** (цены, фандинги, спреды, ликвидность, новости) — через вызов **bot.process_input(input_text)**.
2. **Вопрос:** один раз выводится текст:
   - «Открыть позиции в лонг и шорт? Ведите 'Да' или 'Нет': если 'Да', то позиции будут открыты и введите min цену (через .) закр, для отправки сообщения в тг и закрытия всех позиций».
3. **Ответ пользователя:** одна строка вида «Да, 1» или «Да, 0.5».
   - «Да» (или yes/y) + число — число интерпретируется как **min цену закр** в **процентах** (1 = 1%, 0.5 = 0.5%); разделитель дробной части — точка.
   - «Нет» — бот завершает работу без открытия и мониторинга.
4. **При «Да, X»:** вызываются **open_long_short_positions** (из position_opener) с теми же аргументами, что в bot.py; затем запускается цикл мониторинга.
5. **Мониторинг:** интервал как в **bot.py** (1 с). Строка лога в формате:
   - `🚩 Спред закр: -0.026% | ⛳ Откр: -0.052% | 💰 Фанд: -0.004 (L: 472 мин | S: 472 мин) | 🎯 Общ: -0.055 ⚙️ Long mexc | Short bybit | STO`
   - L / S — минуты до выплаты фандинга на бирже Long и Short (если API возвращает next_funding_time; иначе N/A).
6. **Условие срабатывания:** по **модулю** спреда закрытия: когда **|Спред закр| ≤ X** (введённое значение X в %), выполняется:
   - отправка сообщения в Telegram (канал **FREE_CHANNEL_ID**, тот же формат, что в bot.py при закрытии);
   - закрытие **только этой пары** (Long на одной бирже, Short на другой) через **close_long_short_positions**;
   - выход из мониторинга (один срабатывание — одно закрытие, без логики «3 уведомления» как в bot.py).

### Зависимости

- **bot.PerpArbitrageBot** — process_input, get_futures_data, calculate_opening_spread, calculate_closing_spread, calculate_funding_spread, get_exit_threshold_pct; для L/S минут — вызов get_funding_info по обеим биржам (если есть).
- **input_parser.parse_input** — разбор строки ввода.
- **position_opener.open_long_short_positions**, **position_opener.close_long_short_positions** — открытие и закрытие пары.
- **telegram_sender.TelegramSender**, **config** (канал для уведомлений о закрытии — как в bot.py).

### Запуск

```bash
python scan_fundings_spreads_bot.py
```

Либо с аргументом: `python scan_fundings_spreads_bot.py "STO Long (mexc), Short (bybit) 30"`. Один запуск = одна пара, один цикл «анализ → вопрос → открытие (при «Да») → мониторинг → закрытие при выполнении условия».
