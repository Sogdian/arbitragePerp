# Контекст: Бот для скальпинга на отрицательном фандинге (fun.py)

## Цель стратегии

Получить funding, не неся направленного риска цены.

Это достигается так:

- Ты открываешь короткую позицию (short) сразу после момента выплаты фандинга.
- Ты держишь её минимально возможное время (секунды).
- Ты закрываешь её максимально быстро.

При этом ты стараешься:

- войти по цене, выгоднее «справедливой»,
- выйти с минимальным проскальзыванием,
- не платить funding,
- не ловить импульс против себя.

То есть это не арбитраж между биржами, а локальный микроскальп вокруг funding-момента.

## Логика работы бота

### Формат команды

Пользователь вводит команду вида:

```
"COIN Bybit QTY OFFSET%"
```

например: `"STO Bybit 30 -0.3%"`

Где:
- **COIN** — монета,
- **QTY** — количество монет,
- **OFFSET%** — дополнительный отступ от справедливой цены вниз.

### Что делает бот

Бот:

1. **Получает из API:**
   - `funding_rate`,
   - `next_funding_time`,
   - тикер,
   - стакан,
   - фильтры инструмента (tick, qty step, min qty).

2. **Проверяет:**
   - что монета существует,
   - что хватает ликвидности,
   - что хватает USDT,
   - что нет делистинга / security-новостей,
   - что количество валидно для биржи.

3. **Перед выплатой:**
   - Синхронизируется с серверным временем биржи.
   - За ~1 секунду до payout фиксирует:
     - последнюю цену (trade / last / kline),
     - best bid / ask,
     - стакан,
     - predicted funding.
   - В HH:MM:59 вычисляет:
     - `close_price` = цена фиксации
     - `total_pct` = |funding| + |offset|
     - `fair_price` = close_price - close_price × total_pct
     
     Это и есть справедливая цена входа.

4. **Сразу после payout (например, +10 мс):**
   - Бот выставляет один лимитный Sell IOC:
     - по цене `best_bid_fix - N×tick`
     - чтобы гарантировать мгновенный fill
     - без дополнительных API-запросов в этот момент
   - Цель: получить short после выплаты → не платить funding.

5. **Через короткую паузу (например, +1 сек):**
   - Бот начинает закрывать short:
     - Buy IOC reduce-only,
     - при необходимости — несколько попыток,
     - с эскалацией,
     - цель — выйти максимально быстро.

6. **После завершения:**
   - Бот собирает executions,
   - считает:
     - среднюю цену входа,
     - среднюю цену выхода,
     - комиссии,
     - итоговый PnL в USDT,
   - выводит отчёт.

## Ключевые принципы

1. **Нет угадывания рынка.** Вход не зависит от «куда пойдёт цена», а только от математики funding.

2. **Минимальное время в позиции.** Ты не держишь позицию минуты — только секунды.

3. **Никаких market-ордеров по умолчанию.** Только лимит + IOC. Market допустим лишь как аварийный fallback.

4. **Серверное время, а не локальное.** Все тайминги привязаны к времени биржи.

5. **Одна попытка входа.** Не «долбить» рынок — один точный выстрел.

6. **Цена считается от зафиксированного момента.** Никаких `get_ticker()` в критическую миллисекунду.

7. **Вход после payout, а не до.** Ты не платишь funding — ты его «скальпируешь» через перекос цены.

## Алгоритм действий бота

### 1. Подготовка (до времени выплаты)

- **Проверка ликвидности и новостей:** Используются существующие модули проверки ликвидности и мониторинга новостей/делистинга
- **Pre-flight проверки:** Проверка баланса, минимальных/максимальных размеров, шагов цены и количества, ликвидности на нужных уровнях стакана
- **Логирование:** После успешных проверок записывается соответствующий лог

### 1.1. Тестовые открытия (опционально)

- **Вопрос:** После успешных проверок бот выводит вопрос: **"Совершить тестовые открытия шорт и лонг?"**
  - Если пользователь отвечает **"Да"** → выполнить тестовые открытия
  - Если пользователь отвечает **"Нет"** или что-то другое → пропустить тестовые открытия и перейти к вопросу о реальных позициях

- **Процесс тестовых открытий (если пользователь ответил "Да"):**
  1. **Запрос минимального количества:** Запросить у биржи минимальное количество монет для открытия Short ордера
  2. **Открытие тестового Short:**
     - Открыть тестовый Short ордер по **лучшей цене bid** из стакана на минимальное количество монет
     - Проверить, что Short ордер открылся (позиция создана)
  3. **Закрытие тестового Short:**
     - Закрыть тестовый Short ордер по лучшей цене ask из стакана
     - Проверить успешное закрытие позиции

- **После тестовых открытий:** Если тестовые открытия выполнены успешно, перейти к вопросу о реальных позициях

### 1.2. Интерактивное подтверждение реальных позиций

- **Вопрос:** После тестовых открытий (или если они были пропущены) бот выводит вопрос: **"Открывать позиции лонг и шорт?"**
  - Если пользователь отвечает **"Да"** → скрипт ждет назначенного времени и выполняет сделку
  - Если пользователь отвечает **"Нет"** или что-то другое → скрипт ничего не делает и завершается

- **Ожидание времени выплаты:** Бот готовится к работе к времени выплаты, полученному из API
- **Проверка времени:** Если скрипт запущен после времени выплаты → завершать с ошибкой

### 2. Фиксация цены

- **Момент фиксации:** Ровно в **HH:MM:59** (за 1 секунду до выплаты фандинга)
- **Источник close_price (для сделки):** в приоритете берём **реальную цену в HH:MM:59**:
  - last trade до этого времени (`/v5/market/recent-trade`)
  - fallback: `ticker last`
  - **kline** (`/v5/market/kline`) используется как best-effort "для сравнения" (диагностика), но **не должен затирать** реальную цену в HH:MM:59
  - Важно: большие интервалы `FUN_FIX_KLINE_INTERVAL` (например `60`) часто возвращают **предыдущую завершённую свечу**, а не цену прямо в HH:MM:59
- **Учет тени (wick):** `k_high` (high свечи) логируется отдельно. Что считать `close_price` для расчётов — настраивается:
  - `FUN_FIX_PRICE_MODE=last` (по умолчанию): берём trade/last в момент HH:MM:59 (обычно соответствует "цене на графике" в этот момент)
  - `FUN_FIX_PRICE_MODE=kline_close`: берём close свечи
  - `FUN_FIX_PRICE_MODE=kline_high`: берём high (тень)
  - `FUN_FIX_PRICE_MODE=max`: берём максимум из источников (самый агрессивный вариант)
- **Важно:** Все запросы цены выполняются **до** времени выплаты, чтобы не тормозить выставление ордера в момент payout

### 3. Расчет уровней

- **Справедливая цена (fair_price):** Рассчитывается как цена закрытия минус сумма абсолютных значений funding и offset в процентах от цены закрытия
  - Формула: `fair_price = close_price - (close_price × (|funding_pct| + |offset_pct|))`
  - Пример: цена 0.10412 USDT, фандинг -0.941%, отступ -0.2%
    - Сумма процентов: `0.941% + 0.2% = 1.141%` (в десятичном: `0.00941 + 0.002 = 0.01141`)
    - Вычитание: `0.10412 × 0.01141 = 0.00118801`
    - Справедливая цена: `0.10412 - 0.00118801 = 0.10293 USDT`

### 4. Сделка (БОЕВОЙ режим) — только Short, максимально быстро

Цель: **payout+10ms** открыть Short, **payout+1sec** начать закрывать Short. Никаких "открыть Long" в боевом режиме — закрытие делается **reduceOnly Buy**.

#### 4.1. Подготовка заранее (до выплаты)

- За `FUN_FAST_PREP_LEAD_SEC` секунд до выплаты (по умолчанию `2.0`) делаем подготовку биржи (isolated/leverage=1, best-effort) и получаем фильтры шага цены/количества.
- Ровно в **HH:MM:59** (за секунду до выплаты) делаем:
  - обновление `funding_pct` из API (UI может менять значение прямо перед выплатой)
  - фиксацию `close_price`
  - снимок стакана (orderbook)
  - вычисление **лимита для открытия Short** (агрессивно): берём цену на уровне bids, где **cum_bids >= qty** (в пределах первых `FUN_SHORT_OPEN_LEVELS`, максимум 10 уровней)

#### 4.2. Открытие Short (по server-time Bybit)

Критично не доверять локальным часам: в момент выплаты важны сотни миллисекунд.

- Бот получает Bybit server time через `/v5/market/time`
- Оценивает смещение: `offset_ms = server_ms - local_ms`
- Все ожидания строятся от server timestamp выплаты (`nextFundingTime` из `/v5/market/tickers`):
  - `local_target_ms = server_target_ms - offset_ms`
  - пример для открытия: `open_local_ms = (payout_server_ms + FUN_FAST_OPEN_LEAD_MS) - offset_ms`

**Открытие Short:**
- Выставляется **`Sell Limit IOC`** в момент `payout_server_ms + FUN_FAST_OPEN_LEAD_MS` (по умолчанию 10ms после выплаты)
- В критический момент скрипт не делает лишние запросы/логи
- **Важно про fill:** `Sell limit` должен быть **<= best_bid**, иначе IOC отменится. Поэтому лимит открытия берётся как `min(fair_price, best_bid_fix - N×tick)`
- **Fallback открытия (опционально):** если `Sell IOC` отменился без fill, можно включить `FUN_SHORT_OPEN_FALLBACK_MARKET=1` (по умолчанию 0), тогда бот отправит `Sell Market`

#### 4.3. Закрытие Short (начиная с payout+1sec)

- В **payout+1sec** начинается закрытие Short:
  - сначала берём размер текущей Short‑позиции через `/v5/position/list`
  - затем делаем **несколько** попыток закрытия (до 15 попыток или пока позиция не станет 0)
    - `Buy Limit IOC + reduceOnly=True` по цене asks, подобранной так, чтобы **cum_asks >= remaining**
  - важно: закрытие — приоритетнее "красивой цены", главное **закрыться**.

## Формат запуска бота

### Команда

```bash
python fun.py "STO Bybit 30 -0.3%"
```

### Параметры команды

1. **STO** — монета (тикер)
2. **Bybit** — биржа (пока поддержан только Bybit)
3. **30** — количество монет в сделке (для Short)
4. **-0.3%** — дополнительный отступ (offset) в процентах (отрицательное значение)

**Важно:** `funding_pct` и время следующей выплаты берутся **из API Bybit автоматически**.

### Валидация формата

- Поддерживается **только указанный формат** команды
- **Регистр:** Не важен (например, "STO" = "sto" = "Sto")
- **Пробелы:** Строго один пробел между параметрами
- **Формат процентов:** Только с знаком `%` (например, `-0.3%`)
- **Знак процентов:** Только отрицательные значения (например, `-0.3%`)
- **Проверка биржи:** Достаточно проверить наличие биржи в списке поддерживаемых
- **Проверка времени:** Если текущее время уже позже времени выплаты → ошибка
- Валидация других параметров (например, проверка отрицательности фандинга) **не требуется**

## Практический пример

### Условия:
- Монета: **STO**
- Биржа: **Bybit**
- Количество: **30 монет**
- Offset: **-0.3%**

### Сценарий:
1. **Подготовка** — проверка ликвидности, новостей, баланса
2. **09:59:58** — подготовка биржи + фильтры (best-effort)
3. **09:59:59** — фиксация `close_price` и расчёт `fair_price`
4. **10:00:00.010** — выставляем **Sell IOC** (Short) без доп. запросов
5. **10:00:01** — начинаем закрывать Short: **Buy reduceOnly IOC**, до 15 попыток

## Технические детали

### Время

- **Часовой пояс:** Серверное время биржи (не локальное)
- **Точность:** Фиксация цены в HH:MM:59, открытие Short в payout+10ms, старт закрытия в payout+1sec
- **Синхронизация:** Использование `/v5/market/time` для получения server time и расчета offset
- **Режим работы:** Скрипт работает только на **1 сделку** на конкретное время выплаты фандинга
- **Прерывание скрипта:** Если пользователь прервал скрипт (Ctrl+C) во время ожидания времени выплаты → просто завершить (без отмены ордеров, если они были выставлены)

### Обработка ошибок

1. **Short не открылся (позиция=0):** бот завершает сделку без закрытия.
2. **Short открылся частично:** бот всё равно пытается закрыть фактический размер позиции, считанный с `/v5/position/list`.
3. **Не удалось закрыть Short полностью:** возвращаем ошибку и оставляем лог `❌ Не удалось закрыть Short полностью`.
4. **Скрипт запущен после времени выплаты:** завершаем с ошибкой.

### Интеграция с существующим кодом

1. **position_opener.py:**
   - **Переиспользовать код** для открытия позиций
   - Использовать функции планирования и размещения ордеров
   - Адаптировать под требования IOC (для Short) и частичного исполнения (для закрытия)
   - **Переиспользовать функции закрытия** для закрытия позиций

2. **Модули проверки:**
   - **Проверка ликвидности:** Использовать существующую логику из `scan_spreads.py`
   - **Мониторинг новостей/делистинга:** Использовать `news_monitor.py` и `announcements_monitor.py`
   - **Последовательность:** Сначала проверки → если все ОК → вопрос о тестовых открытиях → после тестовых открытий (или пропуска) → вопрос о реальных позициях → если "Да" → ожидание времени → исполнение

### Логирование

- **Уровень детализации:** Средний
- **Что логировать:**
  - Результаты проверок ликвидности и новостей
  - Запрос минимального количества монет для тестовых открытий
  - Тестовые открытия Short (если выполнялись)
  - Результаты закрытия тестовых позиций
  - Время фиксации цены и саму цену
  - Расчет справедливой цены
  - Проверка Last price перед открытием (payout)
  - Попытки открытия Short
  - Результаты исполнения ордеров
  - Проверка что ордер открыл позицию (после выставления)
  - Попытки закрытия Short по уровням стакана (до 15)
  - Статус проверки закрытия (максимально часто, без задержки)
  - Ошибки и предупреждения
  - **Итоговые логи PnL:** После завершения всех сделок (тестовых и боевых) логируется итоговый PnL с расчетом из истории исполнений
- **Telegram уведомления:** Не нужны

### Расчет PnL

После завершения всех сделок (открытия и закрытия позиций) рассчитывается итоговый PnL на основе истории исполнений (executions) с биржи:

- **Источник данных:** Запрос к `/v5/execution/list` для получения всех исполнений за период сделки
- **Формула расчета:**
  ```
  PnL = sell_notional - buy_notional - комиссии
  комиссии = (количество_покупок + количество_продаж) × 0.005 USDT
  ```
  - `sell_notional` = сумма всех (цена_исполнения × количество) для Sell ордеров
  - `buy_notional` = сумма всех (цена_исполнения × количество) для Buy ордеров
  - Комиссия: **0.005 USDT** за каждую исполненную сделку (execution)

- **Формат итогового лога:**
  ```
  📊 Итог (ТЕСТ/БОЕВОЙ): монета=RESOLV | ср_цена_покупки=0.10274 | ср_цена_продажи=0.10281 | покупок=1 продаж=1 | PnL_USDT_итого=-0.007
  ```
  - Все параметры на русском языке
  - `ср_цена_покупки` - средняя цена покупки (VWAP всех Buy исполнений)
  - `ср_цена_продажи` - средняя цена продажи (VWAP всех Sell исполнений)
  - `покупок` - количество Buy исполнений
  - `продаж` - количество Sell исполнений
  - `PnL_USDT_итого` - итоговый PnL в USDT с учетом комиссий (округление до 3 знаков после запятой, без trailing zeros)

- **Важно:** Расчет PnL выполняется **только после** завершения всех критических операций (открытия и закрытия позиций), чтобы не влиять на скорость исполнения сделок

### Мониторинг

- **Мониторинг позиций после открытия:** Не нужен
- **Stop-loss для Short:** Не используется (закрытие происходит через reduceOnly Buy IOC)

### Размер позиции

- **Минимальный/максимальный размер:** Определяется из параметра команды (количество монет)
- **Проверка:** Выполняется на этапе pre-flight проверок

## Структура скрипта

### Основные компоненты

1. **Парсер команды:** Разбор формата `"STO Bybit 30 -0.3%"`
2. **Получение данных из API:** funding_rate, next_funding_time, тикер, стакан, фильтры
3. **Проверки:** Ликвидность, новости, делистинг, баланс, параметры
4. **Тестовые открытия (опционально):** Вопрос "Совершить тестовые открытия шорт и лонг?" → выполнение тестовых открытий и закрытий
5. **Интерактивное подтверждение:** Вопрос "Открывать позиции лонг и шорт?"
6. **Синхронизация времени:** Получение server time и расчет offset
7. **Ожидание времени:** Точная синхронизация с серверным временем биржи (с обработкой Ctrl+C)
8. **Фиксация цены:** Получение last price в HH:MM:59
9. **Расчет уровней:** Справедливая цена (fair_price)
10. **Открытие Short:** Параллельное открытие Short в payout+10ms (Sell Limit IOC)
11. **Проверка открытия позиции:** Убедиться, что ордер действительно открыл позицию (после выставления)
12. **Закрытие Short:** Начиная с payout+1sec, несколько попыток закрытия (Buy IOC reduce-only, до 15 попыток)
13. **Обработка частичного закрытия:** Продолжение попыток закрыть позицию, если она закрылась частично
14. **Завершение:** Скрипт завершается после успешного закрытия позиции
15. **Расчет PnL:** После завершения всех операций рассчитывается итоговый PnL

### Зависимости

- Использование существующих модулей бирж (`exchanges/`)
- Переиспользование `position_opener.py` для открытия позиций
- Интеграция с модулями проверки ликвидности и новостей

## Примеры использования

### Пример 1: Базовая сделка

```bash
python fun.py "STO Bybit 30 -0.3%"
```

### Пример 2: Другая монета

```bash
python fun.py "GPS Bybit 5111 -0.3%"
```

## Важные замечания

1. **Однократное исполнение:** Скрипт работает только на одну сделку в указанное время выплаты
2. **Серверное время:** Все временные метки используют серверное время биржи (не локальное)
3. **Тип ордеров:** 
   - **Short:** IOC (допускается частичное исполнение)
   - **Закрытие:** IOC reduce-only (допускается частичное исполнение)
4. **Ручное управление:** Если что-то пошло не так, пользователь будет действовать вручную
5. **Интеграция:** Максимальное переиспользование существующего кода для надежности
6. **Одна попытка входа:** Не "долбить" рынок — один точный выстрел после payout
7. **Минимальное время в позиции:** Позиция держится только секунды, не минуты
